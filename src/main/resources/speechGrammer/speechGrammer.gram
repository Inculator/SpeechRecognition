grammar javax.speech.demo;

public <sentence> = hello world | good morning | 
                                      hello mighty computer | one;
                                     
public <number> = (one | two | three | four | five);
public <action> = (plus | minus | terminate);
public <final> = (show result);

public <calculator> = (one | two | three | four | five)
							(plus)
							(one | two | three | four | five);